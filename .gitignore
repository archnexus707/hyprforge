# Per-run install logs
Install-Logs/

# Source-build scratch dir
build/

# Dotfiles bundle
Hyprland-Dots-*.tar.gz
archnexus707-Hyprland-Dots/

# Timestamped backups from tag refresh
**/hypr-tags.env.bak-*

# Stray logs at root
*.log

# Vendor debs (tracked in repo — uncomment to ignore)
# Kali-Hyprland/assets/*.deb

# Font bundles left after failed cleanup
*.tar.xz
FantasqueSansMono.zip
VictorMonoAll.zip

# Backups + caches
*.bak
*.backup-*
.cache/

# Editor / OS noise
*.swp
*.swo
*~
