# Per-run install logs (generated by install.sh, recovery.sh, doctor.sh).
Install-Logs/

# Source-build scratch dir used by dry-run-build.sh and the individual
# build phases (hyprland.sh, hyprutils.sh, …).
build/

# Dotfiles bundle downloaded by install-scripts/dotfiles.sh.
Hyprland-Dots-*.tar.gz
JaKooLit-Hyprland-Dots/

# Backups + caches.
*.bak
*.backup-*
.cache/

# Editor / OS noise.
*.swp
*.swo
.DS_Store
Thumbs.db
