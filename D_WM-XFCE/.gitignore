# Per-run install logs (generated by install.sh, recovery.sh, doctor.sh).
Install-Logs/

# Source-build scratch dir (picom-ftlabs is built from source).
build/

# Per-session backups created by install.sh / uninstall.sh.
# Lives under ~/.dwm-backup/ — not in-tree, but listed here for clarity.

# Backups + caches.
*.bak
*.backup-*
.cache/

# Editor / OS noise.
*.swp
*.swo
.DS_Store
Thumbs.db
